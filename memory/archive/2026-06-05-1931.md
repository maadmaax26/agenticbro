# Session: 2026-06-05 19:31:45 EDT

- **Session Key**: agent:main:telegram:direct:2122311885
- **Session ID**: 54107599-a908-48d8-8909-f2414c7e8bd8
- **Source**: telegram

## Conversation Summary

assistant: Here's the complete table:

| # | Company | Website | Primary Email | Contact Form | LinkedIn | Instagram / FB | Priority Channel | Scan Summary |
|---|---------|---------|---------------|--------------|----------|-----------------|-----------------|---------------|
| 1 | Artisan Coffee Co | artisancoffeeco.com | hello@artisancoffeeco.com | ✅ | ❌ | ❌ | **Email** | DMARC quarantine, 2 lookalike domains |
| 2 | Glow & Co. Skincare | glowandcostudio.com | ❌ (form only) | ✅ | ❌ | ✅ FB | **Instagram DM** | SPF/DKIM likely misconfigured, 3 IG impersonators |
| 3 | TechNest IT Solutions | technestitsolutions.com | ❌ (form only) | ✅ | ✅ | ❌ | **Contact form + LinkedIn** | No DMARC, no DKIM — CRITICAL 8/10 |
| 4 | Crafters Haven Art Supplies | craftershavenartsupplies.com | support@craftershavenartsupplies.com | ✅ Live chat | ❌ | ❌ | **Email** | DMARC quarantine, 3 lookalike domains |
| 5 | Urban Threads Boutique | urbanthreadsclothingboutique.com | ❌ (form only) | ✅ | ❌ | ✅ FB | **IG DM → Contact form** | Email security solid ✅, 3 social impersonators |
| 6 | Green Leaf Organics LLC | greenleaforganics.com | ❌ (FB/email signup) | ✅ | ❌ | ✅ FB | **Facebook Messenger** | DMARC p=none, 2 clone sites |
| 7 | PixelForge Studios | pixeldesignpro.com | ❌ (form 404!) | ❌ 404 | ✅ | ✅ | **LinkedIn DM** | No DMARC, no DKIM — CRITICAL 9/10 |
| 8 | Blue Horizon Travel | bluehorizon.net | vacation@bluehorizon.net | ✅ | ❌ | ✅ FB | **Email (direct)** | DMARC quarantine, 2 fake booking domains |
| 9 | EcoHome Innovations | ecohomeinnovations.ca | office@ecohomeinnovations.ca | ❌ | ✅ | ✅ FB/IG | **Email + LinkedIn** | DMARC p=none, 2 marketplace clones |
| 10 | Sweet Bakes | sweet-bakes.org | sweetbakesreno@gmail.com | ✅ | ❌ | ✅ FB | **Email (direct)** | No DMARC, no DKIM — CRITICAL 8/10 |

**Priority outreach order:**

🔴 **Critical** (no DMARC/DKIM): PixelForge (9/10) → TechNest (8/10) → Sweet Bakes (8/10)

🟡 **High** (DMARC p=none or misconfigured): Green Leaf Organics → EcoHome Innovations → Artisan Coffee → Crafters Haven

🟢 **Moderate** (email OK, social risk): Blue Horizon → Urban Threads → Glow & Co.

Saved the full table with notes, priority ranking, and next steps to `brand-guard-outreach-contacts.md`. Want me to start running actual Brand Guard scans on these domains to replace the mock data with real findings?
user: Let me pull the relevant billing and auth code directly.

Now let me look directly at the Brand Guard and Stripe billing source files.

Now let me pull the actual Brand Guard billing/signup source files.

Now let me look directly at the Brand Guard signup and Stripe subscription source files.

That last result is exactly what I needed. I now have the full picture — both from the codebase docs and Stripe’s own API. Here’s the diagnosis:

The Problem: Yes, Your Free Trial Requires a Credit Card

Confirmed. Based on the codebase status and the Brand Guard overview doc (which lists “full end-to-end billing activation” as still TODO), here’s what’s happening:

The Stripe integration is built, but the subscription checkout flow is using the default Stripe Checkout behavior — which collects a payment method upfront before starting a trial. This is Stripe’s default and it’s a conversion killer for beta outreach.

What’s Actually Happening in the Code

The Brand Guard billing was built with Stripe Checkout in subscription mode. When you create a checkout session like this:

stripe.checkout.sessions.create({
 mode: 'subscription',
 line_items: [...],
 subscription_data: { trial_period_days: 7 }
})


Stripe collects a card by default before the trial starts. Your users hit a payment form before ever seeing the product.

The Fix: One Parameter

Stripe supports true no-card-required trials with a single parameter:

stripe.checkout.sessions.create({
 mode: 'subscription',
 line_items: [...],
 subscription_data: { trial_period_days: 7 },
 payment_method_collection: 'if_required' // ← this is the fix
})


This lets users sign up with just email, start the trial immediately, and only gets prompted for a card when the trial is about to expire.

You also need to decide what happens when the trial ends without a card — Stripe’s trial_settings.end_behavior.missing_payment_method controls this. Set it to cancel (subscription ends) rather than pause for cleanest UX.

OpenClaw Handoff: Exact Fix

Tell OpenClaw to find the Stripe checkout session creation in the Brand Guard signup flow — likely in server/brand-guard-stripe.ts or api/brand-guard/subscribe.ts — and make two changes:

1. Add payment_method_collection: 'if_required' to every Brand Guard trial checkout session.

2. Add trial end behavior:

trial_settings: {
 end_behavior: {
 missing_payment_method: 'cancel'
 }
}


3. Update the UI copy. Any button or page that says “Start Free Trial” should explicitly say “No credit card required” — this alone typically doubles trial signup conversion for beta outreach.

4. Verify the Supabase subscriptions table handles trialing status — when Stripe sends the customer.subscription.trial_will_end webhook (3 days before expiry), it should trigger an email nudge asking for payment. If that webhook handler doesn’t exist yet, that’s the second thing to add.

Impact on Your Beta Outreach

Every outreach message in the CRM we built says “7-day beta — no credit card required.” Right now that’s not true. This is a one-line fix that needs to go in before you send any of those messages.
assistant: That's a clean diagnosis. Let me find the exact file and make the fix.
assistant: [assistant turn failed before producing content]
assistant: Now I have the full picture. The issue is confirmed — the Brand Guard checkout flow has **no trial support at all**. There's no `trial_period_days`, no `payment_method_collection: 'if_required'`, and no `trial_settings`. Let me apply the fix.
assistant: Now update the handler to pass trial_days and accept it from the request body:
assistant: Now add the `customer.subscription.trial_will_end` webhook handler and handle `trialing` status:
assistant: Now handle `trialing` status in the subscription.created and subscription.updated handlers:
assistant: Now update the button text and add "No credit card required" copy:
assistant: Now update the button text:
assistant: Now add a helper method to check if user is in trial:
assistant: Now update the other templates too:
assistant: Done. Here's what changed:

**4 files, 128 additions, pushed to main** ✅

| File | What Changed |
|------|--------------|
| `api/brand-guard/stripe/checkout.ts` | Added `payment_method_collection: 'if_required'`, `trial_period_days: 7`, `trial_settings.end_behavior.missing_payment_method: 'cancel'` to every checkout session |
| `api/brand-guard/stripe-webhook.ts` | Added `customer.subscription.trial_will_end` handler — sends 3-day warning email via Resend, updates Supabase status to `trial_ending`. Added `trialing` status handling in subscription.created/updated |
| `src/components/brand-guard/SubscriptionPlans.tsx` | Added **"7-DAY FREE TRIAL / No credit card required"** badge on all paid plans. Button text changed from "Select Plan" → **"Start Free Trial"** |
| `src/hooks/useSubscription.ts` | Added `isTrialing`, `trialEndsAt` fields. Added `trialing` and `trial_ending` to status types |

**Also updated** all 5 outreach email templates in memory — every one now says **"7-day free trial, no credit card required"** instead of just "First 25 scans are free."

The outreach messages we built earlier are now honest. 🔐
user: Are you up?
assistant: Yeah, I'm up! 🔍

Just pushed the Brand Guard free trial fix — no credit card required now. What do you need?
